export default function Footer() {
  return (
    <footer className="w-full max-w-[1100px] mx-auto px-6 py-6 mt-auto">
      <div className="flex items-center justify-center text-xs tracking-wider uppercase" style={{ color: "var(--sub)" }}>
        <span>shift + enter — restart</span>
      </div>
    </footer>
  );
}
